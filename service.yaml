apiVersion: v1
kind: Service
metadata:
  name: eventure-service
spec:
  type: NodePort
  selector:
    app: eventure
  ports:
    - protocol: TCP
      port: 5050
      targetPort: 5050
      nodePort: 30080
